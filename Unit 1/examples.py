catagories = ['action','comedy','horror','drama']
print(catagories[2])

#binary search - a method of searching where you split the array into halves repeatedly. - list must be ordered

#linear search - A method or searching where you go down a straight incremental path. - list must be ordered

#algorithm is a concise list or orders and instructions

class ttProfile:    
    def __init__(self, username,ttFollowers, videosWatched, FavVideos, saves, likeCount):
        self.username = username
        self.ttFollowers = ttFollowers
        self.videosWatched = videosWatched
        self.FavVideos = FavVideos
        self.saves = saves
        self.likeCount = likeCount
        self.ogContent= ogContent
        
    def postVideo(self):
      video= input("upload video...")
    if videoFormat == 'avi':
            print("do some thing")
    elif videoFormat == 'mp4':
            print("do some thing")
    else:
         print("video format not supported")
    def likeVideo(self, userProfile, video):
        def adds(self, videosByHashtag, videoContent, videoWatchTime, eyeMovement):
         

profile001= ttProfile('Ian',0,0,0,0,0,0,0)
profile002= ttProfile()